"""G-Assist Plugin Protocol V2 transport and command layer.

Filled in by plan tasks 14-16 (framing/transport, lifecycle & dispatch, confirmation wiring,
entry point). Kept import-free at package level so core modules never depend on it.
"""
